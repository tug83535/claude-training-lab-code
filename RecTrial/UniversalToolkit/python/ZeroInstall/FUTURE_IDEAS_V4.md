# FUTURE_IDEAS_V4

Version: 1.0.0

- Add a single command that runs all 6 scripts in sequence with one config file.
- Add a guided prompt mode so coworkers answer questions instead of passing CLI arguments.
- Add output quality scoring (row counts, null checks, stale data flags).
- Add a standard "run receipt" JSON for audit and troubleshooting.
- Add optional Excel hyperlink output index for all generated files.
